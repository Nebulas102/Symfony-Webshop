{% extends 'base.html.twig' %}

{% block title %}UnitMeasure{% endblock %}

{% block body %}
    <h1>UnitMeasure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unit_measure.id }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ unit_measure.unit }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ unit_measure.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unit_measure_index') }}">back to list</a>

    <a href="{{ path('unit_measure_edit', {'id': unit_measure.id}) }}">edit</a>

    {{ include('unit_measure/_delete_form.html.twig') }}
{% endblock %}
